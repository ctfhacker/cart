[package]
name = "cart-benches"
version = "0.0.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Criterion-based benchmarks for cart-core (stub)"
publish = false

[dependencies]
cart-core = { path = "../cart-core" }

[dev-dependencies]
criterion = { version = "0.5", default-features = false, features = ["cargo_bench_support"] }

[[bench]]
name = "codec"
harness = false
