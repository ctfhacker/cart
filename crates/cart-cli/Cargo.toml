[package]
name = "cart-cli"
version = "0.0.0"
edition = "2024"
license = "MIT OR Apache-2.0"
description = "Command-line interface for cart-core (stub)"
publish = false

[dependencies]
cart-core = { path = "../cart-core" }
